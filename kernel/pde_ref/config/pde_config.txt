# Default configuration for Vail Iris Blood Orchid PDE solver

# Grid parameters
width=100
height=100

# Spatial and temporal discretization
dx=0.1
dy=0.1
dt=0.001

# Physical parameters
alpha=0.1  # diffusion coefficient
velocity=1.0  # for advection

# Simulation control
steps=1000
output_interval=100

# Output settings
output_dir=./output
output_format=csv

# Solver selection
solver_type=heat_equation