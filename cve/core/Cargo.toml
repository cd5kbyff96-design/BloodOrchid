[package]
name = "cve-core"
version = "0.1.0"
edition = "2021"

[dependencies]
boundary-runtime = { path = "../../boundary/runtime" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rayon = "1.10"

[lib]
name = "cve_core"
path = "src/lib.rs"

