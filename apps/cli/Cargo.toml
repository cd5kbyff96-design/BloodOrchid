[package]
name = "mves-cli"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
boundary-runtime = { path = "../../boundary/runtime" }
cve-core = { path = "../../cve/core" }

[lib]
name = "mves_cli"
path = "src/lib.rs"

[[bin]]
name = "mves_demo"
path = "src/main.rs"

